apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    fluid: "true"
    category: system
  annotations:
    fluidhq.io/display: "Calico CNI"
  name: calico
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
  project: default
  source:
    path: calico
    repoURL: "https://github.com/aseitnetworks/fluid-k8s-apps"
    targetRevision: production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
